## Count total # of concepts.
 
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>

SELECT (COUNT(?concept) AS ?concept_count)

            WHERE
            { ?concept a skos:Concept .
            }